<div id="page-menu">
<div id="page-menu-interior">
<%= render "shared/pd4Cabecalho" %>
<%= render "shared/pd4menu" %>
<%= form_for @teste do |f| %>
<h1>Semana<h1>
  
<% option_recipes = options_for_select(Recipe.all.map {|r| [ r.title, r.id,]}) %>
<%= select_tag :week,options_for_select(Week.all.map {|s| [s.first_day]}) , :include_blank => true, :class => "cb_week" %>

<% end %>
</div>
</div>
